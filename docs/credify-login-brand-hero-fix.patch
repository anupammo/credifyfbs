diff --git a/app/globals.css b/app/globals.css
index d3bfd36..64d4c02 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -53,7 +53,14 @@ body {
 
 /* ========= BRAND PANEL (B‑hero) ========= */
 .brand {
-  position: relative;
+  /* Pin the brand to a constant viewport height so the taller "Create account"
+     form can't stretch it (which recentred .brand-hero and made the layout jump
+     when switching tabs). align-self:start stops the grid from stretching this
+     cell to the tall row; sticky keeps it in view while the long form scrolls. */
+  position: sticky;
+  top: 0;
+  align-self: start;
+  height: 100vh;
   overflow: hidden;
   color: var(--white);
   background: linear-gradient(135deg, #0b4030 0%, var(--brand) 55%, #06291d 100%);
